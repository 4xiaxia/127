### 东里村文旅项目 - ADM组件与页面部位对应表
宽度移动端自适应，兼容多尺寸 不是固定死死的，pc端打开的时候，超出的部分以好看的特效背景填充（粒子、弥散、等动效 高级质感）

| 页面名称               | 页面部位                                                                 | ADM组件名称       |
|------------------------|--------------------------------------------------------------------------|-------------------|
| 登录页                 | LOGO下方第一个输入区域（手机号输入框）                                   | Input             |
| 登录页                 | 手机号输入框下方、与「获取验证码按钮」并排的输入框（验证码输入框）       | Input             |
| 登录页                 | 验证码输入框右侧（80px宽的「获取验证码」按钮）                           | Button            |
| 登录页                 | 验证码区域下方的通栏按钮（「手机号登录」主按钮）                         | Button            |
| 登录页                 | 分隔线下方的三个通栏按钮（微信/支付宝/游客登录按钮）                     | Button            |
| 登录页                 | 「获取验证码」按钮内部（倒计时数字展示区域）                             | CountDown         |
| 登录页                 | 页面中间偏下悬浮区域（操作提示：手机号错误/登录成功等）                  | Toast             |
| 登录页                 | 页面中间的白色卡片容器（包含LOGO、输入框、所有按钮）                     | Card              |
| 登录页                 | 「手机号登录」按钮下方、第三方登录按钮上方（分隔区域）                   | Divider           |
| 登录页                 | 验证码输入框+获取验证码按钮的外层布局容器                               | Space             |
| Chat/Agent对话页       | 页面底部的输入框区域（麦克风图标左侧）                                   | Input             |
| Chat/Agent对话页       | 输入框右侧（「发送」按钮）                                               | Button            |
| Chat/Agent对话页       | 输入框内右侧（🎤语音按钮）                                               | Button            |
| Chat/Agent对话页       | 页面右下角（全局Agent悬浮按钮）                                         | FloatButton       |
| Chat/Agent对话页       | 页面底部弹出的半屏容器（Agent对话窗口）                                 | Popup             |
| Chat/Agent对话页       | 消息气泡左侧/Popup内顶部（Agent头像展示）                                | Avatar            |
| Chat/Agent对话页       | 页面中间的消息展示区域（AI/用户聊天记录）                               | List              |
| Chat/Agent对话页       | 消息列表的外层容器（下拉刷新历史消息）                                   | PullRefresh       |
| Chat/Agent对话页       | 输入框/按钮内（麦克风/发送/返回图标）                                   | Icon              |
| 分类首页               | 页面中间的卡片布局区域（2列网格，承载5大文旅分类）                       | Grid              |
| 分类首页               | Grid内的每个网格项（红色景点/游玩导游等分类卡片）                        | Card              |
| 分类首页               | 页面最顶部的导航栏区域（标题+返回按钮）                                 | NavBar            |
| 分类首页               | 页面右下角（固定悬浮的全局Agent入口）                                   | FloatButton       |
| 分类首页               | 每个分类Card的外层容器（点击cute-bounce动效）                            | Transition        |
| 烈士/景点列表页        | 页面最外层（390×844滚动容器）                                           | ScrollView        |
| 烈士/景点列表页        | 页面顶部（导航栏：标题+返回按钮）                                       | NavBar            |
| 烈士/景点列表页        | 列表外层容器（下拉刷新最新数据）                                         | PullRefresh       |
| 烈士/景点列表页        | 页面中间的列表区域（承载所有烈士/景点条目）                             | List              |
| 烈士/景点列表页        | List内的单条条目（展示单条烈士/景点信息）                               | List.Cell         |
| 烈士/景点列表页        | List.Cell左侧（标注「烈士/景点/活动」的小徽章）                          | Badge             |
| 烈士/景点列表页        | List.Cell中间（标题+地址/时间描述文本）                                 | Typography        |
| 烈士/景点列表页        | List.Cell右侧（跳转详情的箭头标识）                                     | Icon              |
| 烈士/景点列表页        | 列表区域中间（无数据时的空状态提示）                                     | Empty             |
| 烈士/景点列表页        | 列表底部（加载更多数据时的加载动画）                                     | Loading           |
| 烈士/景点详情页        | 页面中间的详情容器（包含图片/标题/介绍/音频）                           | Card              |
| 烈士/景点详情页        | Card内顶部（景点/烈士主图展示）                                          | Image             |
| 烈士/景点详情页        | Card内介绍文本下方（音频讲解播放控件）                                   | Audio             |
| 烈士/景点详情页        | Audio控件下方（音频进度控制条）                                         | Slider            |
| 烈士/景点详情页        | Card内图片下方（景点/烈士简介文本）                                     | Typography        |
| 烈士/景点详情页        | Card内标题右侧（标注「已打卡/未打卡」的标签）                            | Tag               |
| 个人中心页             | 页面顶部（用户信息容器：头像+用户名+手机号）                             | Card              |
| 个人中心页             | 页面中间的列表区域（打卡记录/手机号/UID条目）                           | List/List.Cell    |
| 个人中心页             | 用户信息Card右上角（展示打卡总数的徽章）                                 | Badge             |
| 个人中心页             | 打卡记录列表中间（无打卡记录时的空状态提示）                             | Empty             |
| 个人中心页             | 页面中间悬浮（修改信息/退出登录的确认弹窗）                             | Dialog            |

### 表格说明
1. 「页面部位」描述精准对应UI视觉区域，可直接映射到390×844移动端页面的物理位置；
2. 组件名称为Ant Design Mobile（v5）官方标准命名，无自定义别名，可直接查阅官方文档复用；
3. 所有组件均适配乡村场景（弱网/老设备/黏土风格），仅需覆写少量CSS即可实现视觉统一。




方案2
个人中心缺失这些信息大概率是系统临时故障或操作层面的问题，按步骤排查就能解决。

### 可能的核心原因
- 系统维护或数据同步延迟，导致个人信息暂时未加载出来。
- APP/平台版本更新后出现兼容性bug，影响信息展示。
- 登录态失效或账号权限异常，导致部分数据无法调取。



方案3
这个东里村文旅服务平台项目**完全可以用 Layui Mobile 开发**，且在「乡村文旅 WAP 端」这个场景下，Layui Mobile 甚至有独特的适配优势；但需结合项目的设计/交互需求，补充少量自定义开发来覆盖其短板。以下是具体分析和落地建议：

### 一、为什么 Layui Mobile 适配这个项目？
#### 1. 核心优势（贴合乡村文旅项目特点）
| 项目核心需求                | Layui Mobile 适配性                                                                 |
|-----------------------------|-------------------------------------------------------------------------------------|
| 移动端 WAP 端（390×844 尺寸） | 原生适配移动端屏幕，支持响应式布局，严格遵循「移动端宽度约束」，无横向溢出风险       |
| 轻量化/弱网适配             | 体积＜20KB（远小于 Vant/AntD Mobile），乡村弱网环境下加载极快，低配手机也流畅       |
| 低开发门槛                  | 原生 JS 开发，无需学 Vue/React，村集体团队/中小开发者能快速上手、后期易维护         |
| 基础组件全覆盖              | 登录表单、按钮、列表、卡片、弹窗、导航栏等核心组件都有现成实现，满足项目 80% 需求   |
| 老旧浏览器兼容              | 乡村用户常使用安卓老版本浏览器，Layui Mobile 对低版本浏览器兼容性远优于现代组件库   |

#### 2. 项目核心模块的 Layui Mobile 实现可行性
| 项目模块                | Layui Mobile 实现方式                                                                 |
|-------------------------|--------------------------------------------------------------------------------------|
| 登录页（手机号/微信/游客登录） | `form` 模块做输入框 + `button` 模块做登录按钮 + `layer-mobile` 做授权弹窗（微信/支付宝） |
| 分类首页（5个卡片）| `grid` 布局 + 自定义 CSS 实现卡片渐变/黏土线框 + 原生 CSS3 实现 `cute-bounce` 点击动效 |
| 烈士/景点列表页         | `list` 模块做列表项 + `badge` 模块做「烈士」标签 + 原生 JS 实现下拉加载               |
| 烈士/景点详情页         | `card` 模块做信息卡 + 原生 HTML5 Audio API + `slider` 模块做音频进度条                |
| 个人中心（手机号/UID/打卡记录） | `card` 模块展示用户信息 + `list` 模块做打卡记录 + 原生 JS 渲染手机号/UID 等动态数据    |
| 全局 Agent 半屏对话框   | `layer-mobile` 的「底部弹窗」+ 自定义 CSS 实现聊天气泡 + 原生 JS 处理语音/文字交互    |

### 二、需要补充的自定义开发（覆盖 Layui Mobile 短板）
Layui Mobile 是「轻量基础组件库」，项目中的「黏土线框风格、复杂交互、AI 相关功能」无现成组件，需用原生 CSS/JS 补充（成本极低）：

#### 1. 设计风格：黏土线框/渐变/装饰细节
Layui Mobile 默认样式偏简约，需加少量 CSS 实现项目要求的「好看度」：
```css
/* 黏土线框（双层细线条+圆角） */
.clay-card {
  border: 1px solid #e8e8e8; /* 外层浅灰 */
  box-shadow: 0 0 0 2px #fff inset; /* 内层白色，模拟黏土双层线 */
  border-radius: 24px; /* 匹配项目圆角规范 */
}
/* 红色文旅卡片渐变（从上到下淡入） */
.red-gradient {
  background: linear-gradient(to bottom, #fff5f5, #ffebeb);
}
/* 卡片底部小圆点装饰 */
.card-dot::after {
  content: "●●●";
  display: block;
  text-align: center;
  color: #d32f2f; /* 红色分类色 */
  letter-spacing: 8px;
  margin-top: 10px;
}
```

#### 2. 交互动效：cute-bounce/音频播放/关键词监听
- **cute-bounce 点击动效**：原生 CSS3 实现，几行代码即可：
  ```css
  .clay-card:active {
    transform: scale(0.95); /* 缩放至95% */
    box-shadow: 0 2px 8px rgba(0,0,0,0.1); /* 阴影加深 */
    transition: all 0.2s ease;
  }
  ```
- **音频播放进度条**：Layui Mobile 有 `slider` 滑块组件，结合原生 Audio API 绑定进度：
  ```js
  // 音频进度同步
  const audio = new Audio('烈士事迹.mp3');
  layui.use('slider', function(){
    const slider = layui.slider;
    slider.render({
      elem: '#audio-slider',
      max: audio.duration,
      change: function(value){
        audio.currentTime = value; // 拖动滑块调整播放进度
      }
    });
    // 播放时同步滑块进度
    audio.ontimeupdate = function(){
      slider.setValue(audio.currentTime);
    };
  });
  ```
- **关键词监听气泡**：原生 JS 正则匹配输入内容，用 `layer-mobile` 弹出提示：
  ```js
  // 监听输入框，匹配「红色文旅」关键词
  document.querySelector('#chat-input').addEventListener('input', function(e){
    if(/红色文旅/.test(e.target.value)){
      layui.use('layer', function(){
        const layer = layui.layer;
        layer.open({
          type: 9, // 气泡类型
          content: '需要了解红色文旅的景点吗？',
          time: 3000, // 3秒自动消失
          style: 'border-radius: 12px; padding: 8px 16px; font-size: 14px;'
        });
      });
    }
  });
  ```

#### 3. 游客/微信/支付宝登录逻辑
（暂时先做前端样式，后期在接入，此时点击就自动登录，演示版方便）Layui Mobile 无现成的第三方登录组件，但可通过「按钮 + 原生 JS 唤起授权」实现：
```js
// 微信登录按钮点击事件
document.querySelector('#wx-login').addEventListener('click', function(){
  // 调用微信 JS-SDK 唤起授权弹窗（乡村项目可简化为跳转微信授权页）
  window.location.href = 'https://open.weixin.qq.com/connect/oauth2/authorize?xxx';
});
// 游客登录（无需验证，直接跳转）
document.querySelector('#guest-login').addEventListener('click', function(){
  window.location.href = './chat-page.html';
});
```

### 三、最终建议
#### 👉 优先选 Layui Mobile 的场景
如果项目满足以下条件，用 Layui Mobile 是最优解：
1. 核心目标是「快速落地、低成本维护」（比如村集体主导的轻量项目）；
2. 主要用户是乡村居民（低配手机/老浏览器/弱网）；
3. 无「小程序/APP 跨端」需求，仅做 WAP 端。

#### 👉 需要注意的落地细节
1. 引入方式：直接用 CDN 引入 Layui Mobile，无需安装依赖，最快上手：
   ```html
   <!-- 引入 Layui Mobile 核心 CSS/JS -->
   <link rel="stylesheet" href="https://cdn.staticfile.org/layui/2.8.18/css/layui.mobile.css">
   <script src="https://cdn.staticfile.org/layui/2.8.18/layui.mobile.js"></script>
   ```
2. 样式复用：把黏土线框、渐变、动效等封装成通用 CSS 类（如 `.clay-card` `.red-gradient`），各页面复用，减少重复代码；
3. 交互简化：保留「打卡、音频播放、Agent 对话」核心交互，非核心动效（如箭头引导动画）可简化，降低开发成本；
4. 后续扩展：如果后期想加小程序/APP，可先基于 Layui Mobile 完成 WAP 基础版，再迁移到 Taro UI（兼容 Layui 样式）。

### 总结
Layui Mobile 不仅能开发这个项目，还能在「乡村文旅场景」下体现出比 Vant/AntD Mobile 更优的适配性（轻量化、兼容老设备）；唯一需要的是补充少量 CSS/JS 实现设计细节和复杂交互，整体开发成本低、落地快，完全贴合这个项目的定位。




方案4
当然可以用 Ant Design（下文简称 antd），但需要先明确**两个核心区分**——避免用错版本导致适配成本过高：
- 「antd」：Ant Design 核心版，专为 PC 端设计（桌面端组件），直接用于 390×844 的移动端场景会出现样式错乱、交互不适配；
- 「Ant Design Mobile（简称 ADM）」：antd 生态的**移动端专属版本**，适配手机端交互/尺寸，是东里村文旅项目的正确选择。

以下是针对东里村文旅项目的详细分析、适配方案和代码示例：

### 一、核心结论：优先选 Ant Design Mobile（ADM）
ADM 完全适配东里村文旅项目的移动端需求（390×844 尺寸、乡村弱网、黏土风格），相比 Layui Mobile，它的优势是：
| 维度                | Ant Design Mobile (ADM)                | Layui Mobile                          |
|---------------------|----------------------------------------|---------------------------------------|
| 技术栈              | 基于 React（支持 React 18+），TypeScript 友好 | 原生 JS，无框架依赖                   |
| 组件丰富度          | 覆盖移动端全场景（表单、导航、音频、地图适配） | 基础组件为主，复杂组件需自定义        |
| 交互体验            | 贴合移动端手势（滑动、下拉、回弹）| 基础交互，复杂手势需原生 JS 补充      |
| 主题定制            | 支持全局主题/组件样式覆写（适配黏土风格） | 纯 CSS 覆写，灵活但无统一主题体系     |
| 弱网适配            | 内置 loading/骨架屏/错误状态           | 需手动实现弱网提示                    |

如果你的项目是 **React 技术栈**，ADM 是更优选择；如果是原生 JS 栈，Layui Mobile 开发成本更低（无需搭建 React 环境）。

### 二、东里村项目 ADM 适配关键：小尺寸+黏土风格
#### 1. 基础配置（适配 390×844 尺寸）
ADM 默认是响应式布局，需添加全局样式约束尺寸，保证和 Layui Mobile 一致的适配效果：
```css
/* 全局样式：约束页面尺寸，适配 iPhone 14 Pro Max */
html, body {
  width: 390px;
  height: 844px;
  margin: 0 auto;
  overflow: hidden;
  font-family: "PingFang SC", "Microsoft YaHei", sans-serif;
  background-color: #f5f5f5;
}
/* 覆写 ADM 组件默认样式，适配黏土线框 */
.am-card {
  border-radius: 24px !important;
  border: 1px solid #e8e8e8 !important;
  box-shadow: 0 0 0 2px #fff inset !important; /* 黏土双层线 */
}
.am-button {
  border-radius: 16px !important;
  height: 48px !important;
}
.am-input {
  border-radius: 12px !important;
  height: 40px !important;
}
```

#### 2. 登录页核心代码示例（ADM 版）
```tsx
import React, { useState, useRef } from 'react';
import { Button, Input, Card, Toast, CountDown, Divider, Space } from 'antd-mobile';
import { WechatOutlined, AlipayOutlined, UserOutlined } from '@ant-design/icons';
import './login.css'; // 引入上述全局样式

const LoginPage = () => {
  // 手机号/验证码状态
  const [phone, setPhone] = useState('');
  const [code, setCode] = useState('');
  const [countdown, setCountdown] = useState(0);
  const timerRef = useRef<NodeJS.Timeout | null>(null);

  // 手机号验证规则
  const validatePhone = (phone: string) => /^1[3-9]\d{9}$/.test(phone);

  // 获取验证码逻辑
  const getCode = () => {
    if (countdown > 0) return;
    if (!validatePhone(phone)) {
      Toast.show({
        content: '请输入有效的中国大陆手机号',
        duration: 2000,
        style: { color: '#f5222d', borderRadius: 12 },
      });
      return;
    }

    // 开始倒计时
    setCountdown(60);
    timerRef.current = setInterval(() => {
      setCountdown(prev => {
        if (prev <= 1) {
          clearInterval(timerRef.current!);
          return 0;
        }
        return prev - 1;
      });
    }, 1000);

    // 弱网适配：延迟提示
    setTimeout(() => {
      Toast.show({
        content: '验证码已发送至您的手机',
        duration: 2000,
        style: { borderRadius: 12 },
      });
    }, 800);
  };

  // 手机号登录逻辑
  const handlePhoneLogin = () => {
    if (!validatePhone(phone)) {
      Toast.show({ content: '手机号格式错误', duration: 2000 });
      return;
    }
    if (code.length !== 6) {
      Toast.show({ content: '请输入6位验证码', duration: 2000 });
      return;
    }

    // 弱网加载提示
    Toast.show({ content: '登录中...', type: 'loading', duration: 0 });
    // 模拟弱网请求
    setTimeout(() => {
      Toast.hide();
      Toast.show({
        content: '登录成功！欢迎来到东里村',
        duration: 2000,
        style: { color: '#48bb78', borderRadius: 12 },
      });
      // 跳转Chat页面
      setTimeout(() => window.location.href = './chat.html', 2000);
    }, 1500);
  };

  // 第三方登录逻辑
  const handleThirdLogin = (type: 'wx' | 'alipay' | 'guest') => {
    const tips = {
      wx: '正在唤起微信授权...',
      alipay: '正在唤起支付宝授权...',
      guest: '游客登录中...',
    };
    const successTips = {
      wx: '微信登录成功！',
      alipay: '支付宝登录成功！',
      guest: '游客登录成功！',
    };
    const delay = type === 'guest' ? 1000 : 1500;

    Toast.show({ content: tips[type], duration: 1500 });
    setTimeout(() => {
      Toast.show({
        content: successTips[type],
        duration: 2000,
        style: { color: '#48bb78', borderRadius: 12 },
      });
      setTimeout(() => window.location.href = './chat.html', delay);
    }, delay);
  };

  return (
    <div className="login-page">
      {/* 顶部标题栏 */}
      <div className="top-bar">
        <h1>东里村文旅服务平台</h1>
      </div>

      {/* 登录卡片（黏土风格） */}
      <Card className="clay-card">
        {/* LOGO占位 */}
        <div className="logo">🏞️</div>

        {/* 手机号输入 */}
        <div className="form-item">
          <label className="form-label">请输入手机号</label>
          <Input
            type="tel"
            placeholder="请输入11位手机号"
            value={phone}
            onChange={val => setPhone(val)}
            className="form-input"
          />
        </div>

        {/* 验证码输入 + 倒计时按钮 */}
        <div className="form-item">
          <label className="form-label">请输入验证码</label>
          <Space className="code-row">
            <Input
              type="number"
              placeholder="6位验证码"
              value={code}
              onChange={val => setCode(val)}
              className="code-input"
            />
            <Button
              className="get-code-btn"
              disabled={countdown > 0}
              onClick={getCode}
              style={{
                backgroundColor: countdown > 0 ? '#e8e8e8' : '#1677ff',
                borderColor: countdown > 0 ? '#e8e8e8' : '#1677ff',
              }}
            >
              {countdown > 0 ? `重新发送(${countdown})` : '获取验证码'}
            </Button>
          </Space>
        </div>

        {/* 手机号登录按钮 */}
        <Button
          className="login-btn"
          color="primary"
          onClick={handlePhoneLogin}
          style={{ backgroundColor: '#2d3748', marginTop: 15 }}
        >
          手机号登录
        </Button>

        {/* 分隔线 */}
        <Divider className="divider">其他登录方式</Divider>

        {/* 微信登录 */}
        <Button
          className="third-login-btn"
          icon={<WechatOutlined />}
          onClick={() => handleThirdLogin('wx')}
          style={{ backgroundColor: '#07c160', marginBottom: 12 }}
        >
          微信登录
        </Button>

        {/* 支付宝登录 */}
        <Button
          className="third-login-btn"
          icon={<AlipayOutlined />}
          onClick={() => handleThirdLogin('alipay')}
          style={{ backgroundColor: '#1677ff', marginBottom: 12 }}
        >
          支付宝登录
        </Button>

        {/* 游客登录 */}
        <Button
          className="third-login-btn"
          icon={<UserOutlined />}
          onClick={() => handleThirdLogin('guest')}
          style={{ backgroundColor: '#f5f5f5', color: '#4a5568' }}
        >
          游客登录
        </Button>
      </Card>

      {/* 底部协议 */}
      <div className="agreement">
        登录即同意 <a href="javascript:;">《用户服务协议》</a> 和 <a href="javascript:;">《隐私政策》</a>
      </div>
      <div className="copyright">Design by 东里村团队</div>
    </div>
  );
};

export default LoginPage;
```

### 三、使用 ADM 的关键注意事项（贴合乡村场景）
#### 1. 轻量化优化（弱网适配）
ADM 体积比 Layui Mobile 大，需开启**按需引入**（减少加载体积）：
```js
// 配置 babel-plugin-import 实现按需引入
module.exports = {
  plugins: [
    ['import', {
      libraryName: 'antd-mobile',
      style: 'css', // 只引入 CSS
    }],
  ],
};
```

#### 2. 黏土风格适配
通过覆写 ADM 组件的 `className` + CSS，实现和 Layui Mobile 一致的黏土线框效果（核心是 `border + inset box-shadow`）：
```css
.clay-card {
  width: 320px;
  margin: 20px auto;
  padding: 30px 20px;
  border: 1px solid #e8e8e8 !important;
  box-shadow: 0 0 0 2px #fff inset !important;
  border-radius: 24px !important;
}
```

#### 3. 老设备兼容
ADM 基于 React，需兼容乡村用户的老旧安卓浏览器：
- 引入 `core-js` 做 ES6+ 语法兼容；
- 避免使用复杂动效（如骨架屏、渐变动画），减少低端机卡顿。

### 四、最终选型建议
| 技术栈/场景          | 推荐选择               | 核心原因                                  |
|---------------------|------------------------|-------------------------------------------|
| 原生 JS/快速落地     | Layui Mobile           | 零依赖、低学习成本、适配老旧设备          |
| React 技术栈/中大型项目 | Ant Design Mobile      | 组件丰富、TypeScript 友好、交互体验更优   |
| 需跨端（H5+小程序）  | Taro + Taro UI/ADM     | 一套代码适配多端，兼容 ADM 组件           |

简单来说：如果你的项目只是「轻量移动端 H5」，Layui Mobile 足够用且开发更快；如果是 React 技术栈、需要更丰富的交互/组件，ADM 是更好的选择。
Ant Design Mobile（ADM）对**东里村文旅项目**的核心需求覆盖度可拆解为「页面类型、核心功能、适配场景」三个维度，整体能覆盖 **95%以上** 的核心需求，剩余不足5%的小众定制化需求（如极致的黏土风格细节、乡村特有的弱网兜底逻辑），仅需少量自定义 CSS/JS 即可补充，无核心功能缺失。

### 一、核心维度覆盖度拆解
#### 1. 页面类型覆盖：100%（全场景覆盖）
东里村文旅项目的所有核心页面，ADM 均能通过组件组合实现，无页面类型遗漏：
| 项目核心页面       | ADM 覆盖度 | 核心支撑组件                          | 补充说明                          |
|--------------------|------------|---------------------------------------|-----------------------------------|
| 登录页             | 100%       | Input、Button、CountDown、Toast、Card | 仅黏土线框需覆写少量 CSS          |
| Chat/Agent 对话页  | 100%       | Input、Button、Popup、Avatar、List     | 语音识别需对接原生 API（非ADM组件）|
| 分类首页（5大卡片） | 100%       | Grid、Card、FloatButton、NavBar        | 卡片动效需自定义 CSS 实现cute-bounce |
| 烈士/景点列表页   

结合东里村文旅项目的页面布局（390×844 移动端尺寸、黏土风格），以下是 Ant Design Mobile（ADM）核心组件与各页面具体位置的一一对应关系，每个组件都标注「视觉位置+功能作用」，可直接对应到页面UI区域：

### 一、登录页（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `Input` | 1. LOGO下方第一个输入区域（手机号输入框）<br>2. 手机号输入框下方、与「获取验证码按钮」并排的输入框（验证码输入框） | 接收手机号（tel类型）、验证码（number类型）输入，覆写样式实现12px圆角+黏土线框 |
| `Button` | 1. 验证码输入框右侧（80px宽的「获取验证码」按钮）<br>2. 验证码区域下方的通栏按钮（「手机号登录」主按钮）<br>3. 分隔线下方的三个通栏按钮（微信/支付宝/游客登录按钮） | 触发验证码发送、登录提交、第三方授权，自定义颜色（微信绿/支付宝蓝/游客灰）+16px圆角 |
| `CountDown` | 「获取验证码」按钮内部（倒计时数字展示区域） | 替代手动定时器，实现60秒倒计时，按钮置灰时显示“重新发送(XX)” |
| `Toast` | 页面中间偏下悬浮区域 | 提示手机号错误、验证码发送成功、登录中/登录成功等信息，自定义12px圆角+对应颜色（错误红/成功绿） |
| `Card` | 页面中间的白色卡片容器（包含LOGO、输入框、所有按钮） | 作为登录模块的外层容器，覆写样式实现24px圆角+黏土双层线框（border+inset box-shadow） |
| `Divider` | 「手机号登录」按钮下方、第三方登录按钮上方 | 展示“其他登录方式”文本，分隔手机号登录和第三方登录区域，自定义分割线颜色为#e8e8e8 |
| `Space` | 验证码输入框+获取验证码按钮的外层布局容器 | 替代手动flex，实现两者横向排列，固定10px间距，适配320px宽的登录卡片 |

### 二、Chat/Agent 对话页（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `Input` | 页面底部的输入框区域（麦克风图标左侧） | 接收用户文字输入，支持占位符“请输入问题/点击语音”，12px圆角+黏土线框 |
| `Button` | 1. 输入框右侧（「发送」按钮）<br>2. 输入框内右侧（🎤语音按钮）<br>3. 页面右下角（全局Agent悬浮按钮） | 触发消息发送、语音识别、唤起半屏对话框，悬浮按钮实现50px圆形+阴影 |
| `Popup` | 页面底部弹出的半屏容器（占屏幕下半部分） | 作为全局Agent的对话窗口，390px宽+24px顶部圆角+黏土线框 |
| `Avatar` | 1. 对话页顶部（AI消息气泡左侧）<br>2. Popup内顶部（村官小助理头像） | 展示Agent头像，48px圆形+背景色#1677ff，替代纯emoji图标 |
| `List` | 页面中间的消息展示区域（AI/用户消息气泡列表） | 渲染聊天记录，每条消息对应一个List.Item，区分AI（右对齐）/用户（左对齐）气泡 |
| `PullRefresh` | 消息列表的外层容器 | 下拉刷新聊天记录，适配弱网环境，刷新提示自定义为“加载历史消息...” |
| `Icon` | 1. 输入框内（麦克风Icon）<br>2. 发送按钮内（纸飞机Icon）<br>3. 顶部导航栏左侧（返回Icon） | 替代纯文本/emoji，提升视觉辨识度，适配移动端触控尺寸 |

### 三、分类首页（5大文旅卡片）（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `Grid` | 页面中间的卡片布局区域（2列网格） | 承载5大分类卡片（红色景点/游玩导游/乡镇历史/自媒体动态/活动通知），适配390px页面宽度，间距10px |
| `Card` | Grid内的每个网格项（分类卡片） | 每个卡片320px宽（2列时单张180px）+24px圆角+黏土线框，点击触发跳转 |
| `NavBar` | 页面最顶部的导航栏区域 | 展示标题“东里村文旅服务平台”，左侧返回按钮，适配390px宽度+浅绿渐变背景 |
| `FloatButton` | 页面右下角（固定悬浮） | 全局Agent入口，50px圆形+黏土风格+🧑‍💼图标，点击唤起Popup |
| `Transition` | 每个分类Card的外层容器 | 实现点击时的cute-bounce动效（缩放0.95），替代原生CSS动画，更稳定 |

### 四、烈士/景点列表页（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `List/Cell` | 页面中间的列表区域（每条烈士/景点条目） | 展示条目信息：左侧Badge标签+中间标题+右侧箭头，每条Cell底部1px分割线，适配390px宽度 |
| `PullRefresh` | 列表外层容器 | 下拉刷新最新的烈士/景点数据，弱网环境下加载提示“正在刷新...” |
| `Loading` | 列表底部（加载更多时） | 展示加载动画，替代纯文本提示，适配乡村弱网的慢加载场景 |
| `Badge` | 每个List.Cell左侧（小徽章区域） | 标注“烈士”（红色）/“景点”（蓝色）/“活动”（黄色），区分条目类型，提升辨识度 |
| `Empty` | 列表区域中间（无数据时） | 展示空状态提示“暂无相关内容”+图标，替代空白页面，提升用户体验 |

### 五、烈士/景点详情页（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `Card` | 页面中间的详情容器（包含图片/标题/介绍/音频） | 作为详情模块外层，390px宽-20px边距+24px圆角+黏土线框 |
| `Image` | Card内顶部（景点/烈士主图） | 展示图片，支持点击预览（ADM原生预览功能），16px圆角，适配弱网懒加载 |
| `Audio` | Card内介绍文本下方（音频播放控件） | 承载音频讲解，自定义播放/暂停样式，适配移动端音频控制逻辑 |
| `Slider` | Audio控件下方（线性进度条） | 控制音频播放进度，替代原生Audio进度条，12px圆角+黏土风格 |
| `Typography` | Card内图片下方（介绍文本区域） | 展示景点/烈士简介，自定义行高1.6、字体大小14px，提升可读性 |
| `Tag` | Card内标题右侧（小标签区域） | 展示“已打卡”/“未打卡”状态，自定义颜色（绿色/灰色），8px圆角 |

### 六、个人中心页（核心组件+对应位置）
| ADM 组件名 | 对应页面具体位置 | 功能作用 |
|------------|------------------|----------|
| `List/Cell` | 1. 页面中间（打卡记录条目）<br>2. 用户信息卡下方（手机号/UID条目） | 展示打卡时间/景点名称、用户基础信息，每条Cell左文本右值，适配390px宽度 |
| `Card` | 页面顶部（用户信息容器） | 展示头像+用户名+手机号+UID，80px圆形头像+24px圆角+黏土线框 |
| `Badge` | 用户信息Card右上角（小徽章） | 展示打卡总数（如“已打卡3个”），红色背景+白色数字，突出核心信息 |
| `Empty` | 打卡记录List区域中间（无打卡时） | 展示“暂无打卡记录”提示，引导用户去景点打卡，替代空白 |
| `Dialog` | 页面中间悬浮（修改信息/退出登录确认） | 确认类操作弹窗，24px圆角+黏土线框，自定义按钮文字（确认/取消） |

### 核心总结
ADM 组件在东里村文旅项目中是「精准对应页面视觉区域」的，而非笼统复用：
- 表单类组件（Input/Button/CountDown）聚焦登录页的交互核心区；
- 容器类组件（Card/List/Grid）承载各页面的核心内容区；
- 反馈类组件（Toast/Loading/Empty）覆盖全页面的提示场景；
- 交互类组件（Popup/FloatButton/Slider）聚焦用户操作高频区（底部输入、右下角悬浮、音频控制）。

剩余不足5%的定制化需求（如极致的黏土线框细节、乡村弱网兜底），仅需在组件外层加自定义className，覆写少量CSS即可补充，无核心功能缺失。
graph TD
    subgraph 东里村文旅项目-Ant Design Mobile组件映射
        %% 登录页组件
        A[登录页] --> A1[Input | LOGO下方手机号/验证码输入框 | 接收输入+手机号/数字格式限制]
        A --> A2[Button | 验证码右侧/登录主按钮/第三方登录按钮 | 触发操作+自定义颜色/圆角/黏土风格]
        A --> A3[CountDown | 获取验证码按钮内部 | 60秒倒计时+按钮置灰防重复点击]
        A --> A4[Toast | 页面中间偏下悬浮区 | 操作提示（手机号错误/登录成功等）]
        A --> A5[Card | 页面中间白色卡片容器 | 登录模块外层+24px圆角+黏土双层线框]
        A --> A6[Divider | 登录按钮与第三方按钮之间 | 分隔区域+展示「其他登录方式」文本]
        A --> A7[Space | 验证码输入框+按钮外层 | 横向布局+10px固定间距]
        
        %% Chat/Agent对话页组件
        B[Chat/Agent对话页] --> B1[Input | 页面底部输入框（麦克风左侧） | 文字输入+自定义占位符]
        B --> B2[Button | 输入框右侧/右下角悬浮按钮 | 发送消息/语音识别/唤起Agent对话框]
        B --> B3[Popup | 页面底部半屏容器 | Agent半屏对话窗口+390px宽+黏土风格]
        B --> B4[Avatar | 消息气泡左侧/Popup顶部 | 展示Agent头像（48px圆形）]
        B --> B5[List | 页面中间消息区 | 渲染聊天记录+区分AI/用户消息对齐]
        B --> B6[PullRefresh | 消息列表外层 | 下拉刷新历史消息（弱网适配）]
        B --> B7[Icon | 输入框/按钮内 | 麦克风/发送/返回图标（替代emoji）]
        
        %% 分类首页组件
        C[分类首页] --> C1[Grid | 页面中间网格区（2列） | 承载5大文旅分类卡片]
        C --> C2[Card | Grid内每个网格项 | 分类卡片+黏土线框+点击cute-bounce动效]
        C --> C3[NavBar | 页面顶部 | 展示标题+返回按钮+浅绿渐变背景]
        C --> C4[FloatButton | 页面右下角 | 全局Agent悬浮入口（50px圆形）]
        C --> C5[Transition | 分类Card外层 | 点击缩放动效（适配老旧设备）]
        
        %% 烈士/景点列表页组件
        D[烈士/景点列表页] --> D1[List/Cell | 页面中间列表区 | 展示条目+左侧标签+底部分割线]
        D --> D2[PullRefresh | 列表外层容器 | 下拉刷新最新数据（弱网提示）]
        D --> D3[Loading | 列表底部 | 加载更多动画（替代纯文本提示）]
        D --> D4[Badge | List.Cell左侧 | 标注条目类型（烈士/景点/活动）]
        D --> D5[Empty | 列表中间 | 无数据时空状态提示（引导打卡）]
        
        %% 烈士/景点详情页组件
        E[烈士/景点详情页] --> E1[Card | 页面中间详情容器 | 详情模块外层+黏土线框]
        E --> E2[Image | Card内顶部 | 主图展示+点击预览+弱网懒加载]
        E --> E3[Audio | Card内文本下方 | 音频讲解播放控件（原生适配）]
        E --> E4[Slider | Audio下方 | 音频进度控制+12px圆角]
        E --> E5[Typography | Card内图片下方 | 简介文本+行高/字体优化]
        E --> E6[Tag | Card内标题右侧 | 标注打卡状态（已打卡/未打卡）]
        
        %% 个人中心页组件
        F[个人中心页] --> F1[List/Cell | 页面中间列表区 | 展示打卡记录/手机号/UID条目]
        F --> F2[Card | 页面顶部 | 用户信息容器（头像+用户名+手机号）+黏土线框]
        F --> F3[Badge | 用户Card右上角 | 展示打卡总数（如「已打卡3个」）]
        F --> F4[Empty | 打卡列表中间 | 无打卡记录时空状态提示]
        F --> F5[Dialog | 页面中间悬浮 | 修改信息/退出登录确认弹窗（黏土圆角）]
    end


    ——————————




    graph TD
    subgraph 烈士/景点列表页【390×844】
        A[根容器 ScrollView<br>（页面滚动容器）] --> B[顶部导航 NavBar<br>（标题+返回按钮）]
        A --> C[下拉刷新容器 PullRefresh<br>（包裹核心列表）]
        C --> C1[列表容器 List<br>（承载所有条目）]
        C1 --> C11[列表条目 Cell<br>（单条烈士/景点信息）]
        C11 --> C111[标签 Badge<br>（标注「烈士/景点/活动」）]
        C11 --> C112[文本区域 Typography<br>（标题+描述/时间）]
        C11 --> C113[箭头 Icon<br>（跳转详情标识）]
        C1 --> C12[空状态 Empty<br>（无数据时展示）]
        C --> C2[加载更多 Loading<br>（列表底部，弱网加载）]
    end
    ### 东里村文旅项目 - ADM组件与页面部位对应表
| 页面名称               | 页面部位                                                                 | ADM组件名称       |
|------------------------|--------------------------------------------------------------------------|-------------------|
| 登录页                 | LOGO下方第一个输入区域（手机号输入框）                                   | Input             |
| 登录页                 | 手机号输入框下方、与「获取验证码按钮」并排的输入框（验证码输入框）       | Input             |
| 登录页                 | 验证码输入框右侧（80px宽的「获取验证码」按钮）                           | Button            |
| 登录页                 | 验证码区域下方的通栏按钮（「手机号登录」主按钮）                         | Button            |
| 登录页                 | 分隔线下方的三个通栏按钮（微信/支付宝/游客登录按钮）                     | Button            |
| 登录页                 | 「获取验证码」按钮内部（倒计时数字展示区域）                             | CountDown         |
| 登录页                 | 页面中间偏下悬浮区域（操作提示：手机号错误/登录成功等）                  | Toast             |
| 登录页                 | 页面中间的白色卡片容器（包含LOGO、输入框、所有按钮）                     | Card              |
| 登录页                 | 「手机号登录」按钮下方、第三方登录按钮上方（分隔区域）                   | Divider           |
| 登录页                 | 验证码输入框+获取验证码按钮的外层布局容器                               | Space             |
| Chat/Agent对话页       | 页面底部的输入框区域（麦克风图标左侧）                                   | Input             |
| Chat/Agent对话页       | 输入框右侧（「发送」按钮）                                               | Button            |
| Chat/Agent对话页       | 输入框内右侧（🎤语音按钮）                                               | Button            |
| Chat/Agent对话页       | 页面右下角（全局Agent悬浮按钮）                                         | FloatButton       |
| Chat/Agent对话页       | 页面底部弹出的半屏容器（Agent对话窗口）                                 | Popup             |
| Chat/Agent对话页       | 消息气泡左侧/Popup内顶部（Agent头像展示）                                | Avatar            |
| Chat/Agent对话页       | 页面中间的消息展示区域（AI/用户聊天记录）                               | List              |
| Chat/Agent对话页       | 消息列表的外层容器（下拉刷新历史消息）                                   | PullRefresh       |
| Chat/Agent对话页       | 输入框/按钮内（麦克风/发送/返回图标）                                   | Icon              |
| 分类首页               | 页面中间的卡片布局区域（2列网格，承载5大文旅分类）                       | Grid              |
| 分类首页               | Grid内的每个网格项（红色景点/游玩导游等分类卡片）                        | Card              |
| 分类首页               | 页面最顶部的导航栏区域（标题+返回按钮）                                 | NavBar            |
| 分类首页               | 页面右下角（固定悬浮的全局Agent入口）                                   | FloatButton       |
| 分类首页               | 每个分类Card的外层容器（点击cute-bounce动效）                            | Transition        |
| 烈士/景点列表页        | 页面最外层（390×844滚动容器）                                           | ScrollView        |
| 烈士/景点列表页        | 页面顶部（导航栏：标题+返回按钮）                                       | NavBar            |
| 烈士/景点列表页        | 列表外层容器（下拉刷新最新数据）                                         | PullRefresh       |
| 烈士/景点列表页        | 页面中间的列表区域（承载所有烈士/景点条目）                             | List              |
| 烈士/景点列表页        | List内的单条条目（展示单条烈士/景点信息）                               | List.Cell         |
| 烈士/景点列表页        | List.Cell左侧（标注「烈士/景点/活动」的小徽章）                          | Badge             |
| 烈士/景点列表页        | List.Cell中间（标题+地址/时间描述文本）                                 | Typography        |
| 烈士/景点列表页        | List.Cell右侧（跳转详情的箭头标识）                                     | Icon              |
| 烈士/景点列表页        | 列表区域中间（无数据时的空状态提示）                                     | Empty             |
| 烈士/景点列表页        | 列表底部（加载更多数据时的加载动画）                                     | Loading           |
| 烈士/景点详情页        | 页面中间的详情容器（包含图片/标题/介绍/音频）                           | Card              |
| 烈士/景点详情页        | Card内顶部（景点/烈士主图展示）                                          | Image             |
| 烈士/景点详情页        | Card内介绍文本下方（音频讲解播放控件）                                   | Audio             |
| 烈士/景点详情页        | Audio控件下方（音频进度控制条）                                         | Slider            |
| 烈士/景点详情页        | Card内图片下方（景点/烈士简介文本）                                     | Typography        |
| 烈士/景点详情页        | Card内标题右侧（标注「已打卡/未打卡」的标签）                            | Tag               |
| 个人中心页             | 页面顶部（用户信息容器：头像+用户名+手机号）                             | Card              |
| 个人中心页             | 页面中间的列表区域（打卡记录/手机号/UID条目）                           | List/List.Cell    |
| 个人中心页             | 用户信息Card右上角（展示打卡总数的徽章）                                 | Badge             |
| 个人中心页             | 打卡记录列表中间（无打卡记录时的空状态提示）                             | Empty             |
| 个人中心页             | 页面中间悬浮（修改信息/退出登录的确认弹窗）                             | Dialog            |

### 表格说明
1. 「页面部位」描述精准对应UI视觉区域，可直接映射到390×844移动端页面的物理位置；
2. 组件名称为Ant Design Mobile（v5）官方标准命名，无自定义别名，可直接查阅官方文档复用；
3. 所有组件均适配乡村场景（弱网/老设备/黏土风格），仅需覆写少量CSS即可实现视觉统一。