# 东里村智能导游系统 - 产品开发说明书

## 📋 项目概览

### 项目基本信息
- **项目名称**: 东里村智能导游系统 (village-guide-ai-system)
- **项目性质**: 公益扶贫乡村AI导览系统
- **技术定位**: 基于简化ANP协议的多智能体协作架构
- **目标用户**: 乡村游客、文旅管理者、当地商户
- **演示版本**: 支持硬编码，优先考虑落地

### 核心目标
1. **智能导览**: AI问答、语音交互、图像识别、地图导航
2. **内容管理**: 村子介绍、景点信息、东里名人、活动打卡
3. **多智能体协作**: 简化Agent架构，确保可落地

## 🏗️ 系统架构

### 简化Agent架构 (核心创新)
基于第一性原理设计的4角色Agent系统：

```
A（探子） → B（答题器） → C（弹药库） → D（看顾）
```

#### Agent职责划分
| Agent | 角色定位 | 核心职责 | 技术实现 |
|--------|----------|----------|----------|
| **Agent A** | 探子（前端情报官） | 收集用户输入、展示结果、页面导航 | React + Ant Design Mobile |
| **Agent B** | 答题器（工具调用器） | 调用AI/语音/地图API、无脑输出 | API调用 + 工具集成 |
| **Agent C** | 弹药库（数据库管理员） | 管理所有数据、提供查询接口 | JSON文件 + IndexedDB |
| **Agent D** | 看顾（监控员） | 记录日志、监控系统状态 | 简单日志收集 |

## 📱 页面设计规范

### 技术选型：Ant Design Mobile (ADM)
**选择理由**:
- React生态完整，TypeScript友好
- 移动端专用组件，精准映射页面部位（基于2.0antd重构.md详细映射）
- 组件丰富度95%覆盖需求
- 支持主题定制，适配黏土风格
- 响应式布局支持，兼容多种屏幕尺寸

### 页面结构映射表

#### 1. 登录页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| LOGO下方输入框 | `Input` | 手机号输入，tel类型 |
| 验证码输入区 | `Input` + `Button` + `CountDown` | 验证码输入 + 倒计时 |
| 登录按钮 | `Button` | 主登录按钮，深色主题 |
| 第三方登录 | `Button` | 微信/支付宝/游客登录 |
| 提示区域 | `Toast` | 操作反馈，12px圆角 |
| 卡片容器 | `Card` | 白色卡片，24px圆角黏土风格 |

#### 2. 分类首页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| 导航栏 | `NavBar` | 标题 + 返回按钮 |
| 分类网格 | `Grid` | 2列布局，5大文旅分类 |
| 分类卡片 | `Card` + `Transition` | 黏土风格 + cute-bounce动效 |
| 悬浮按钮 | `FloatButton` | 右下角Agent入口 |

#### 3. 烈士/景点列表页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| 滚动容器 | `ScrollView` | 390×844全屏滚动 |
| 列表区域 | `List` + `PullRefresh` | 下拉刷新，弱网适配 |
| 列表条目 | `List.Cell` + `Badge` + `Typography` | 徽章分类 + 文本信息 |
| 空状态 | `Empty` | 无数据提示 |
| 加载更多 | `Loading` | 底部加载动画 |

#### 4. 烈士/景点详情页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| 详情容器 | `Card` | 黏土风格卡片 |
| 主图展示 | `Image` | 支持预览，弱网懒加载 |
| 音频控件 | `Audio` + `Slider` | 原生Audio API + 进度条 |
| 介绍文本 | `Typography` | 简介内容，行高1.6 |
| 打卡标签 | `Tag` | 已打卡/未打卡状态 |

#### 5. Chat/Agent对话页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| 输入区域 | `Input` + `Button` | 文字输入 + 发送按钮 |
| 语音按钮 | `Button` | 🎤语音识别触发 |
| 对话窗口 | `Popup` | 底部半屏对话框 |
| 消息列表 | `List` + `Avatar` | AI/用户消息气泡 |
| 悬浮入口 | `FloatButton` | 全局Agent按钮 |

#### 6. 个人中心页
| 页面部位 | ADM组件 | 功能描述 |
|----------|----------|----------|
| 用户信息卡 | `Card` + `Badge` | 头像 + 基本信息 + 打卡数徽章 |
| 功能列表 | `List.Cell` | 打卡记录/手机号/UID等 |
| 确认弹窗 | `Dialog` | 修改信息/退出登录 |

## 🎨 设计规范

### 黏土风格实现
```css
/* 黏土双层线框 */
.clay-card {
  border: 1px solid #e8e8e8;
  box-shadow: 0 0 0 2px #fff inset;
  border-radius: 24px;
}

/* 红色渐变背景 */
.red-gradient {
  background: linear-gradient(to bottom, #fff5f5, #ffebeb);
}

/* cute-bounce动效 */
.clay-card:active {
  transform: scale(0.95);
  transition: all 0.2s ease;
}
```

### 移动端响应式适配
```css
/* 基础移动端适配 - 支持多种屏幕尺寸 */
html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
  -ms-text-size-adjust: 100%;
}

body {
  margin: 0;
  padding: 0;
  font-family: "PingFang SC", "Microsoft YaHei", "Helvetica Neue", Arial, sans-serif;
  line-height: 1.5;
  color: #333;
  background-color: #f5f5f5;
}

/* 响应式容器 */
.container {
  width: 100%;
  max-width: 428px; /* iPhone Pro Max 最大宽度 */
  margin: 0 auto;
  min-height: 100vh;
  position: relative;
  overflow-x: hidden;
}

/* 小屏幕适配 (iPhone SE ~ 320px) */
@media screen and (max-width: 374px) {
  html {
    font-size: 14px;
  }
  .container {
    max-width: 100%;
  }
}

/* 中等屏幕适配 (iPhone 12 ~ 390px) */
@media screen and (min-width: 375px) and (max-width: 413px) {
  html {
    font-size: 15px;
  }
}

/* 大屏幕适配 (iPhone Pro Max ~ 428px) */
@media screen and (min-width: 414px) {
  html {
    font-size: 16px;
  }
}

/* 平板适配 (iPad ~ 768px) */
@media screen and (min-width: 768px) {
  .container {
    max-width: 768px;
    padding: 0 20px;
  }
  html {
    font-size: 18px;
  }
}

/* PC端适配 - 精美背景填充 */
@media screen and (min-width: 1024px) {
  body {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    background-attachment: fixed;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    position: relative;
    overflow: hidden;
  }

  body::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
      radial-gradient(circle at 20% 50%, rgba(255,255,255,0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, rgba(255,255,255,0.05) 0%, transparent 50%),
      radial-gradient(circle at 40% 20%, rgba(255,255,255,0.08) 0%, transparent 50%);
    animation: floatingBg 20s ease-in-out infinite;
    pointer-events: none;
  }

  .container {
    max-width: 428px; /* 保持移动端最佳宽度 */
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(20px);
    border-radius: 32px;
    box-shadow: 
      0 32px 64px rgba(0,0,0,0.1),
      0 16px 32px rgba(0,0,0,0.08),
      0 8px 16px rgba(0,0,0,0.06),
      inset 0 1px 0 rgba(255,255,255,0.5);
    border: 1px solid rgba(255,255,255,0.2);
    transform: translateY(0);
    transition: all 0.3s ease;
  }

  .container:hover {
    transform: translateY(-2px);
    box-shadow: 
      0 40px 80px rgba(0,0,0,0.15),
      0 20px 40px rgba(0,0,0,0.1),
      0 10px 20px rgba(0,0,0,0.08),
      inset 0 1px 0 rgba(255,255,255,0.6);
  }

  /* 装饰性背景元素 */
  .bg-decoration {
    position: fixed;
    pointer-events: none;
    opacity: 0.3;
  }

  .bg-circle-1 {
    width: 300px;
    height: 300px;
    background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
    border-radius: 50%;
    top: 10%;
    left: 10%;
    animation: float1 15s ease-in-out infinite;
  }

  .bg-circle-2 {
    width: 200px;
    height: 200px;
    background: radial-gradient(circle, rgba(255,255,255,0.08) 0%, transparent 70%);
    border-radius: 50%;
    bottom: 20%;
    right: 15%;
    animation: float2 12s ease-in-out infinite;
  }

  .bg-pattern {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
      linear-gradient(45deg, transparent 49.5%, rgba(255,255,255,0.03) 49.5%, rgba(255,255,255,0.03) 50.5%, transparent 50.5%),
      linear-gradient(-45deg, transparent 49.5%, rgba(255,255,255,0.03) 49.5%, rgba(255,255,255,0.03) 50.5%, transparent 50.5%);
    background-size: 60px 60px;
    animation: slidePattern 30s linear infinite;
  }
}

/* 超大屏幕适配 */
@media screen and (min-width: 1440px) {
  .container {
    max-width: 480px;
  }
  
  .bg-circle-1 {
    width: 400px;
    height: 400px;
  }
  
  .bg-circle-2 {
    width: 300px;
    height: 300px;
  }
}

/* 动画定义 */
@keyframes floatingBg {
  0%, 100% { transform: translate(0, 0) rotate(0deg); }
  33% { transform: translate(30px, -30px) rotate(120deg); }
  66% { transform: translate(-20px, 20px) rotate(240deg); }
}

@keyframes float1 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(20px, -20px) scale(1.05); }
}

@keyframes float2 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  50% { transform: translate(-15px, 15px) scale(0.95); }
}

@keyframes slidePattern {
  0% { background-position: 0 0, 0 0; }
  100% { background-position: 60px 60px, -60px -60px; }
}

/* 安全区域适配 (刘海屏/底部指示器) */
.safe-area {
  padding-top: env(safe-area-inset-top);
  padding-bottom: env(safe-area-inset-bottom);
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
}

/* 视口设置 */
.viewport {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
}
```

## 📊 数据结构

### 核心数据源 (data目录)
| 数据文件 | 内容描述 | 数据量 | 用途 |
|----------|----------|--------|------|
| `village_figures.json` | 村庄人物志 | 革命先辈+乡贤名士+大学生花名册 | Agent C管理 |
| `scenic_spots.json` | 风景景点大全 | 8个景点（自然/历史/民俗等） | Agent C管理 |
| `red_culture.json` | 红色文旅专区 | 4个红色文化景点 | Agent C管理 |
| `event_announcements.json` | 活动公告 | 6个活动（文化节/采摘节等） | Agent C管理 |
| `self_media.json` | 自媒体展示 | 5篇文章（乡村振兴/文化传承） | Agent C管理 |

### 数据模型示例
```typescript
// 景点数据模型
interface SpotData {
  id: string;
  name: string;
  local_name: string;
  type: 'red_culture' | 'natural' | 'folk' | 'agricultural';
  images: string[];
  intro: string;
  story: string;
  audio_url: string;
  can_bookmark: boolean;
  location: string;
}

// 人物数据模型
interface FigureData {
  id: string;
  name: string;
  birth_death: string;
  images: string[];
  title: string;
  deed: string;
  related_spot: string[];
}
```

## 🤖 AI服务集成

### 核心API配置
| 服务商 | API地址 | 密钥 | 用途 |
|--------|----------|------|------|
| 硅基流动 | `https://api.siliconflow.cn` | `sk-xwmofaucrbykmzwwtbdwannjoxzxhssbwcfeafxykkdoouwe` | AI问答（Qwen3-8B） |
| MiniMax | 内置 | `eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...` | 语音合成/识别 |
| 高德地图 | CDN引入 | `8428614f111312a91d57c0651f63e743` | 地图导航 |

### Agent B工具调用实现
```typescript
class AgentB_Answerer {
  async processRequest(request: ScoutRequest) {
    switch(request.type) {
      case 'question':
        return await this.callSiliconFlow(request.data.userInput);
      case 'voice':
        return await this.callMiniMaxTTS(request.data.voiceData);
      case 'navigation':
        return await this.callAmapAPI(request.data.location);
      case 'photo':
        return await this.recognizeImage(request.data.imageData);
    }
  }
}
```

## 🔄 核心业务流程

### 1. 用户登录流程
```
用户输入手机号 → A验证格式 → 发送验证码 → 用户输入验证码 → A验证登录 → 跳转首页
```

### 2. AI问答流程
```
用户输入问题 → A收集情报 → 发送给B → B调用AI API → 返回结果 → A展示给用户
```

### 3. 景点打卡流程
```
用户点击景点 → A进入详情页 → 拍照/听音频 → 触发打卡 → B生成书签 → C记录数据 → A展示成功
```

### 4. 语音交互流程
```
用户点击语音 → A录音 → 发送给B → B语音识别+AI处理 → B语音合成 → A播放结果
```

## 🛠️ 开发实现

### 项目结构
```
village-guide-clean/
├── src/
│   ├── components/          # React组件 (ADM)
│   ├── core/              # 简化Agent系统
│   │   ├── agents/        # A/B/C/D四个Agent
│   │   └── network.ts    # ANP通信协议
│   ├── services/          # API服务调用
│   ├── data/              # 静态数据文件
│   └── utils/             # 工具函数
├── assets/                 # 静态资源
├── public/                 # 公共文件
└── package.json           # 项目配置
```

### 核心依赖
```json
{
  "dependencies": {
    "react": "^18.3.1",
    "antd-mobile": "^5.41.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
```

### 开发命令
```bash
# 安装依赖
npm install

# 启动开发
npm run dev

# 构建生产
npm run build
```

## 📱 移动端优化

### 弱网适配
- 图片懒加载
- 音频预加载
- 离线数据缓存
- 优雅降级提示

### 老设备兼容
- ES6语法兼容 (core-js)
- 减少复杂动画
- 触摸优化
- 内存管理

### 性能优化
- 代码分割
- 组件懒加载
- 图片压缩
- CDN加速

## 🧪 测试策略

### 功能测试
- [ ] 登录流程完整测试
- [ ] AI问答准确性测试
- [ ] 语音交互测试
- [ ] 地图导航测试
- [ ] 打卡功能测试

### 兼容性测试
- [ ] iOS Safari测试
- [ ] Android Chrome测试
- [ ] 微信浏览器测试
- [ ] 弱网环境测试

### 用户体验测试
- [ ] 操作流程顺畅度
- [ ] 响应速度测试
- [ ] 错误处理测试
- [ ] 界面美观度测试

## 📈 部署上线

### 构建配置
```typescript
// vite.config.ts
export default {
  build: {
    target: 'es2015',
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor': ['react', 'antd-mobile'],
          'utils': ['./src/utils']
        }
      }
    }
  }
}
```

### 环境变量
```bash
# .env.production
VITE_APP_TITLE=东里村智能导游
VITE_API_BASE_URL=https://api.donglivillage.com
VITE_AMAP_KEY=8428614f111312a91d57c0651f63e743
```

## 🎯 项目里程碑

### Phase 1: 核心框架 (1-2周)
- [x] 项目结构搭建
- [x] Agent架构设计
- [ ] 基础组件开发
- [ ] 登录页面实现

### Phase 2: 核心功能 (2-3周)
- [ ] Agent通信机制
- [ ] AI问答功能
- [ ] 数据管理模块
- [ ] 首页分类导航

### Phase 3: 完整功能 (2-3周)
- [ ] 语音交互功能
- [ ] 地图导航功能
- [ ] 打卡书签功能
- [ ] 个人中心页面

### Phase 4: 优化上线 (1周)
- [ ] 性能优化
- [ ] 兼容性测试
- [ ] 部署配置
- [ ] 用户培训

## 🔧 开发规范

### 代码规范
- TypeScript严格模式
- ESLint + Prettier
- 组件化开发
- 注释覆盖率80%+

### Git规范
- feat: 新功能
- fix: 修复bug
- docs: 文档更新
- style: 代码格式调整

### 提交规范
```
feat(agent): 添加Agent B语音处理功能
fix(ui): 修复登录页面验证码倒计时问题
docs(api): 更新AI服务接口文档
```

## 📞 技术支持

### 关键文档
- [Ant Design Mobile官方文档](https://mobile.ant.design/)
- [简化Agent架构设计](./简化Agent架构设计.md)
- [项目结构说明](./PROJECT_STRUCTURE.md)

### 问题反馈
- 技术问题: GitHub Issues
- 业务需求: 产品需求文档
- 紧急问题: 电话联系

---

**注意**: 本项目为演示版本，支持硬编码实现，优先考虑落地效果而非安全防护。所有技术选型均基于乡村实际使用场景，确保在弱网、老设备环境下稳定运行。
